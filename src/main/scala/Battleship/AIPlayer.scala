/*case class AIPlayer(name: String, ships: List[Ship] = List()) extends Battleship.Player {

    def isAlive: Boolean = {
        // TODO: Implement
        true
    }

    def shot(cell: Cell, gameState: GameState): GameState = {
        // TODO: IMPLEMENT
        GameState(gameState.player1, gameState.player2)
    }

    override def createShips(typeShips: List[TypeShip], player: Battleship.Player, f1:() => String, f2:() => Int, f3:() => Int): AIPlayer = {
        AIPlayer("test")
    }
}*/
